var firstNumber;
var secondNumber;

if(typeof(firstNumber) == typeof(secondNumber) ){
    document.write("the variables are same type");
} else{
    document.write("the variables are not the same");
}